import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { ClipboardCheck, FileDown, FileSpreadsheet, FileWarning, LockKeyhole, Plus, ShieldCheck } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";

type FinancialRow = { code: string; label: string; value: string };
const incidentLabels = { GOVERNANCE: "Perubahan tata kelola", OFFICE_OR_OUTLET: "Kantor / gerai", BUSINESS_DISRUPTION: "Gangguan usaha", FORCE_MAJEURE: "Force majeure", COOPERATION: "Kerja sama", REGULATOR_REQUEST: "Permintaan regulator", OTHER: "Lainnya" } as const;
type IncidentCategory = keyof typeof incidentLabels;

function today() { return new Date().toISOString().slice(0, 10); }
function firstDayOfYear() { return `${new Date().getFullYear()}-01-01`; }
function parseRows(raw: string, group: string) {
  const rows = raw.split("\n").map((line) => line.trim()).filter(Boolean).map((line) => { const [code, label, value, ...extra] = line.split("|").map((part) => part.trim()); if (!code || !label || !value || extra.length) throw new Error(`${group}: gunakan format KODE | NAMA POS | NILAI.`); return { code, label, value }; });
  if (!rows.length) throw new Error(`${group} minimal satu baris.`);
  return rows;
}
function statusTone(status: string) { return ({ DRAFT: "status-inactive", PREPARED: "status-pending", APPROVED: "status-approved", EXPORTED: "status-exported" } as Record<string, string>)[status] ?? "status-inactive"; }

function printIncident(item: { reportNumber: string; category: IncidentCategory; incidentAt: Date | string; title: string; description: string; evidenceReference?: string | null; initialAction?: string | null }) {
  const popup = window.open("", "_blank"); if (!popup) { toast.error("Izinkan pop-up browser untuk mencetak register."); return; }
  popup.document.write(`<!doctype html><title>${item.reportNumber}</title><style>body{font-family:Arial,sans-serif;margin:32px;color:#18395f;max-width:800px}h1{font-size:22px}dt{font-weight:bold;margin-top:14px}dd{margin:5px 0;white-space:pre-wrap}.notice{margin-top:24px;border:2px solid #a86b16;padding:12px;color:#7d510b;font-weight:700}</style><h1>Register Laporan Insidental — ${item.reportNumber}</h1><p>PT Ibukota Valasindo · ${new Date().toLocaleString("id-ID")}</p><dl><dt>Kategori</dt><dd>${incidentLabels[item.category]}</dd><dt>Waktu kejadian</dt><dd>${new Date(item.incidentAt).toLocaleString("id-ID")}</dd><dt>Judul</dt><dd>${item.title}</dd><dt>Uraian</dt><dd>${item.description}</dd><dt>Referensi bukti</dt><dd>${item.evidenceReference || "—"}</dd><dt>Tindakan awal</dt><dd>${item.initialAction || "—"}</dd></dl><p class="notice">REGISTER INTERNAL / PAKET MANUAL — Petugas berwenang harus menilai kewajiban laporan, melengkapi format terbaru, dan melakukan unggah melalui kanal resmi. Aplikasi ini tidak mengirim data ke regulator.</p><script>window.print()</script>`); popup.document.close();
}

export default function RegulatoryReportingAdvanced() {
  const { user } = useAuth(); const utils = trpc.useUtils();
  const financialSnapshots = trpc.regulatoryReports.financialSnapshots.list.useQuery(); const incidents = trpc.regulatoryReports.incidents.list.useQuery();
  const [periodStart, setPeriodStart] = useState(firstDayOfYear); const [periodEnd, setPeriodEnd] = useState(today); const [sourceLabel, setSourceLabel] = useState(""); const [sourceReference, setSourceReference] = useState(""); const [profitLoss, setProfitLoss] = useState(""); const [balanceSheet, setBalanceSheet] = useState(""); const [equity, setEquity] = useState(""); const [importedSource, setImportedSource] = useState<{ sourceStorageKey: string | null; sourceFileName: string; sourceMimeType: string } | null>(null); const [isImporting, setIsImporting] = useState(false);
  const [incident, setIncident] = useState({ category: "OTHER" as IncidentCategory, incidentAt: today(), discoveredAt: today(), title: "", description: "", evidenceReference: "", initialAction: "" }); const [notes, setNotes] = useState<Record<number, string>>({});
  const refresh = () => Promise.all([financialSnapshots.refetch(), incidents.refetch(), utils.regulatoryReports.readiness.invalidate(), utils.regulatoryReports.list.invalidate()]);
  const createSnapshot = trpc.regulatoryReports.financialSnapshots.create.useMutation({ onSuccess: () => { toast.success("Snapshot keuangan tersimpan dan siap diperiksa."); setSourceLabel(""); setSourceReference(""); setProfitLoss(""); setBalanceSheet(""); setEquity(""); void refresh(); }, onError: (error) => toast.error(error.message) });
  const createFinancialDraft = trpc.regulatoryReports.createFinancialDraft.useMutation({ onSuccess: () => { toast.success("Draf paket keuangan dibuat dari snapshot terkendali."); void refresh(); }, onError: (error) => toast.error(error.message) });
  const createIncident = trpc.regulatoryReports.incidents.create.useMutation({ onSuccess: () => { toast.success("Register insidental dibuat sebagai draf internal."); setIncident({ category: "OTHER", incidentAt: today(), discoveredAt: today(), title: "", description: "", evidenceReference: "", initialAction: "" }); void refresh(); }, onError: (error) => toast.error(error.message) });
  const prepareIncident = trpc.regulatoryReports.incidents.prepare.useMutation({ onSuccess: () => { toast.success("Register siap diperiksa Shareholder."); void refresh(); }, onError: (error) => toast.error(error.message) });
  const approveIncident = trpc.regulatoryReports.incidents.approve.useMutation({ onSuccess: () => { toast.success("Register insidental disetujui dan terkunci."); void refresh(); }, onError: (error) => toast.error(error.message) });
  const exportIncident = trpc.regulatoryReports.incidents.markExported.useMutation({ onSuccess: () => void refresh(), onError: (error) => toast.error(error.message) });
  const uploadSpreadsheet = async (file: File | undefined) => { if (!file) return; if (file.size > 5 * 1024 * 1024) { toast.error("Ukuran berkas impor maksimal 5 MB."); return; } setIsImporting(true); try { const dataBase64 = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = () => reject(new Error("Berkas tidak dapat dibaca.")); reader.readAsDataURL(file); }); const inferredMime = file.type || (file.name.toLowerCase().endsWith(".xls") ? "application/vnd.ms-excel" : "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"); const response = await fetch("/api/financial-snapshot-import", { method: "POST", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ dataBase64, originalFileName: file.name, mimeType: inferredMime, byteSize: file.size }) }); const payload = await response.json() as { imported?: { profitLossRows: FinancialRow[]; balanceSheetRows: FinancialRow[]; equityRows: FinancialRow[]; sourceStorageKey: string | null; sourceFileName: string; sourceMimeType: string }; message?: string }; if (!response.ok || !payload.imported) throw new Error(payload.message || "Berkas tidak dapat diimpor."); const toText = (rows: FinancialRow[]) => rows.map((row) => `${row.code} | ${row.label} | ${row.value}`).join("\n"); setProfitLoss(toText(payload.imported.profitLossRows)); setBalanceSheet(toText(payload.imported.balanceSheetRows)); setEquity(toText(payload.imported.equityRows)); setSourceLabel(`Impor ${payload.imported.sourceFileName}`); setSourceReference(`Pemetaan nonpersisten dari ${payload.imported.sourceFileName}; simpan berkas asli pada arsip perusahaan.`); setImportedSource({ sourceStorageKey: payload.imported.sourceStorageKey, sourceFileName: payload.imported.sourceFileName, sourceMimeType: payload.imported.sourceMimeType }); toast.success("Pos B0002, B0003, dan B0004 berhasil dipetakan tanpa menyimpan berkas. Periksa kembali sebelum menyimpan snapshot."); } catch (error) { toast.error(error instanceof Error ? error.message : "Berkas tidak dapat diimpor."); } finally { setIsImporting(false); } };
  const submitFinancial = (event: FormEvent) => { event.preventDefault(); try { const payload = { periodStart: new Date(`${periodStart}T00:00:00`), periodEnd: new Date(`${periodEnd}T23:59:59.999`), sourceLabel, sourceReference: sourceReference || undefined, sourceStorageKey: importedSource?.sourceStorageKey || undefined, sourceFileName: importedSource?.sourceFileName, sourceMimeType: importedSource?.sourceMimeType, profitLossRows: parseRows(profitLoss, "Laba rugi"), balanceSheetRows: parseRows(balanceSheet, "Neraca"), equityRows: parseRows(equity, "Perubahan ekuitas") }; createSnapshot.mutate(payload); } catch (error) { toast.error(error instanceof Error ? error.message : "Format snapshot tidak valid."); } };
  const submitIncident = (event: FormEvent) => { event.preventDefault(); createIncident.mutate({ ...incident, incidentAt: new Date(`${incident.incidentAt}T00:00:00`), discoveredAt: new Date(`${incident.discoveredAt}T00:00:00`), evidenceReference: incident.evidenceReference || undefined, initialAction: incident.initialAction || undefined }); };

  return <section className="space-y-6">
    <WorkbookBundleImport onImported={(imported, sourceNames) => { const toText = (rows: FinancialRow[]) => rows.map((row) => `${row.code} | ${row.label} | ${row.value}`).join("\n"); setProfitLoss(toText(imported.profitLossRows)); setBalanceSheet(toText(imported.balanceSheetRows)); setEquity(toText(imported.equityRows)); setSourceLabel(`Gabungan ${sourceNames.join(" + ")}`.slice(0, 180)); setSourceReference(`Pemetaan nonpersisten dari sumber terpisah: ${sourceNames.join(", ")}. Simpan berkas asli pada arsip perusahaan.`); setImportedSource({ sourceStorageKey: imported.sourceStorageKey, sourceFileName: imported.sourceFileName, sourceMimeType: imported.sourceMimeType }); }} />
    <a href="/api/financial-snapshot-template" className="press-scale inline-flex h-9 items-center justify-center rounded-md border border-[#bcd1e5] bg-white px-3 text-sm font-semibold text-[#315879] hover:bg-[#f4f9ff]"><FileDown className="mr-1.5 size-4" />Unduh template B0002–B0004</a>
    <RoleWalkthroughGuide />
    <Card className="border-[#dce6f0]"><CardHeader><div className="flex gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-[#e9f1fb] text-[#35628f]"><FileSpreadsheet className="size-5" /></span><div><CardTitle className="font-display text-xl text-[#18395f]">3. Snapshot laporan keuangan terkendali</CardTitle><CardDescription>Impor berkas XLSX/XLS berisi FORM B0002, B0003, dan B0004, atau masukkan pos yang telah direkonsiliasi. Satu baris memakai format <strong>KODE | NAMA POS | NILAI</strong>.</CardDescription></div></div></CardHeader><CardContent><form onSubmit={submitFinancial} className="space-y-4"><div className="flex flex-col gap-3 rounded-xl border border-dashed border-[#bcd1e5] bg-[#f8fbff] p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold text-[#315879]">Impor FORM B0002 / B0003 / B0004</p><p className="mt-1 text-xs leading-5 text-[#647a92]">Maksimal 5 MB. Sistem memetakan kolom Record No, Pos Akun, dan Nilai; periksa hasilnya sebelum menyimpan.</p></div><label className="press-scale inline-flex h-9 cursor-pointer items-center justify-center rounded-md bg-[#e7f49a] px-3 text-sm font-semibold text-[#203a56] hover:bg-[#dff085]"><FileSpreadsheet className="mr-1.5 size-4" />{isImporting ? "Mengimpor…" : "Pilih XLSX / XLS"}<input type="file" className="sr-only" accept=".xlsx,.xls,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel" disabled={isImporting} onChange={(event) => { void uploadSpreadsheet(event.target.files?.[0]); event.currentTarget.value = ""; }} /></label></div><div className="grid gap-4 lg:grid-cols-4"><Field label="Awal periode" type="date" value={periodStart} onChange={setPeriodStart} /><Field label="Akhir periode" type="date" value={periodEnd} onChange={setPeriodEnd} /><Field label="Nama sumber" placeholder="Contoh: Trial balance direkonsiliasi" value={sourceLabel} onChange={setSourceLabel} /><Field label="Referensi sumber" placeholder="Nomor file / kertas kerja" value={sourceReference} onChange={setSourceReference} /></div><div className="grid gap-4 lg:grid-cols-3"><RowField label="Laba rugi — B0003" value={profitLoss} onChange={setProfitLoss} placeholder="01 | Penjualan UKA | 0" /><RowField label="Neraca — B0002" value={balanceSheet} onChange={setBalanceSheet} placeholder="101 | Kas Rupiah | 0" /><RowField label="Perubahan ekuitas — B0004" value={equity} onChange={setEquity} placeholder="01 | Saldo positif | 0" /></div><div className="flex flex-col gap-3 rounded-xl bg-[#f5f8fc] p-4 sm:flex-row sm:items-center sm:justify-between"><p className="text-xs leading-5 text-[#60758d]">Aplikasi memeriksa kode ganda, nilai desimal, dan kelengkapan tiga kelompok. Angka ini tidak menyesuaikan bon, kas, atau saldo produksi.</p><Button type="submit" disabled={createSnapshot.isPending} className="bg-[#183f70] text-white hover:bg-[#12345d]"><Plus className="mr-2 size-4" />Simpan snapshot</Button></div></form><div className="mt-5 space-y-3">{financialSnapshots.isLoading ? <div className="h-20 animate-pulse rounded-xl bg-[#f3f6fa]" /> : null}{!financialSnapshots.isLoading && !financialSnapshots.data?.length ? <p className="rounded-xl border border-dashed border-[#cfdbe7] p-4 text-sm text-[#718297]">Belum ada snapshot keuangan. Masukkan angka yang telah direkonsiliasi sebelum membuat paket laporan keuangan.</p> : null}{financialSnapshots.data?.map((snapshot) => { const summary = snapshot.validationSummary as { counts?: { profitLoss?: number; balanceSheet?: number; equity?: number }; warnings?: string[] }; return <div key={snapshot.id} className="flex flex-col gap-3 rounded-xl border border-[#e0e8f1] bg-white p-4 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap gap-2"><p className="font-bold text-[#294866]">{snapshot.sourceLabel}</p><Badge variant="outline">{new Date(snapshot.periodStart).toLocaleDateString("id-ID")}–{new Date(snapshot.periodEnd).toLocaleDateString("id-ID")}</Badge>{snapshot.sourceFileName ? <Badge className="bg-[#edf4fb] text-[#386185] hover:bg-[#edf4fb]">{snapshot.sourceFileName}</Badge> : null}</div><p className="mt-2 text-xs text-[#718297]">{summary.counts?.profitLoss ?? 0} pos laba/rugi · {summary.counts?.balanceSheet ?? 0} pos neraca · {summary.counts?.equity ?? 0} pos ekuitas</p>{summary.warnings?.map((warning) => <p key={warning} className="mt-1 text-xs text-amber-700">{warning}</p>)}</div><Button size="sm" disabled={createFinancialDraft.isPending} onClick={() => createFinancialDraft.mutate({ snapshotId: snapshot.id })} className="bg-[#183f70] text-white hover:bg-[#12345d]"><FileDown className="mr-1.5 size-3.5" />Buat draf keuangan</Button></div>;})}</div></CardContent></Card>

    <Card className="border-[#dce6f0]"><CardHeader><div className="flex gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-indigo-100 text-indigo-700"><FileWarning className="size-5" /></span><div><CardTitle className="font-display text-xl text-[#18395f]">4. Register laporan insidental</CardTitle><CardDescription>Catat kejadian dan bukti internal lebih dahulu. Petugas berwenang tetap menentukan apakah, kapan, dan dengan format apa kejadian harus dilaporkan.</CardDescription></div></div></CardHeader><CardContent><form onSubmit={submitIncident} className="grid gap-4 lg:grid-cols-2"><div><Label>Kategori</Label><select value={incident.category} onChange={(event) => setIncident((current) => ({ ...current, category: event.target.value as IncidentCategory }))} className="mt-2 flex h-10 w-full rounded-md border border-input bg-white px-3 text-sm ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><option value="GOVERNANCE">Perubahan tata kelola</option><option value="OFFICE_OR_OUTLET">Kantor / gerai</option><option value="BUSINESS_DISRUPTION">Gangguan usaha</option><option value="FORCE_MAJEURE">Force majeure</option><option value="COOPERATION">Kerja sama</option><option value="REGULATOR_REQUEST">Permintaan regulator</option><option value="OTHER">Lainnya</option></select></div><Field label="Judul kejadian" value={incident.title} onChange={(value) => setIncident((current) => ({ ...current, title: value }))} /><Field label="Tanggal kejadian" type="date" value={incident.incidentAt} onChange={(value) => setIncident((current) => ({ ...current, incidentAt: value }))} /><Field label="Tanggal diketahui" type="date" value={incident.discoveredAt} onChange={(value) => setIncident((current) => ({ ...current, discoveredAt: value }))} /><div className="lg:col-span-2"><RowField label="Uraian kejadian" value={incident.description} onChange={(value) => setIncident((current) => ({ ...current, description: value }))} placeholder="Kronologi singkat, fakta yang diketahui, dan dampak operasional." /></div><div><RowField label="Referensi bukti internal" value={incident.evidenceReference} onChange={(value) => setIncident((current) => ({ ...current, evidenceReference: value }))} placeholder="Nomor notulen, tiket, dokumen, atau folder bukti." /></div><div><RowField label="Tindakan awal" value={incident.initialAction} onChange={(value) => setIncident((current) => ({ ...current, initialAction: value }))} placeholder="Langkah pengamanan atau tindak lanjut yang telah dilakukan." /></div><div className="lg:col-span-2"><Button type="submit" disabled={createIncident.isPending} className="bg-[#183f70] text-white hover:bg-[#12345d]"><Plus className="mr-2 size-4" />Buat draf register</Button></div></form><div className="mt-6 space-y-3">{incidents.isLoading ? <div className="h-24 animate-pulse rounded-xl bg-[#f3f6fa]" /> : null}{!incidents.isLoading && !incidents.data?.length ? <p className="rounded-xl border border-dashed border-[#cfdbe7] p-4 text-sm text-[#718297]">Belum ada register insidental. Gunakan hanya untuk kejadian nyata yang perlu diperiksa petugas.</p> : null}{incidents.data?.map((item) => { const canApprove = user?.role === "SHAREHOLDER" && item.status === "PREPARED" && item.preparedByUserId !== user.id; return <article key={item.id} className="rounded-xl border border-[#e0e8f1] bg-white p-4"><div className="flex flex-col gap-3 xl:flex-row xl:justify-between"><div><div className="flex flex-wrap gap-2"><p className="font-bold text-[#294866]">{item.reportNumber}</p><Badge className={statusTone(item.status)}>{item.status}</Badge><Badge variant="outline">{incidentLabels[item.category]}</Badge></div><p className="mt-2 text-sm text-[#586f88]">{item.title}</p><p className="mt-1 text-xs text-[#718297]">Kejadian: {new Date(item.incidentAt).toLocaleString("id-ID")}</p></div><div className="flex flex-wrap gap-2">{item.status === "DRAFT" ? <Button size="sm" onClick={() => prepareIncident.mutate({ incidentId: item.id })}><ClipboardCheck className="mr-1.5 size-3.5" />Siapkan</Button> : null}{canApprove ? <Button size="sm" onClick={() => approveIncident.mutate({ incidentId: item.id, notes: notes[item.id] || undefined })} className="bg-emerald-700 text-white hover:bg-emerald-800"><ShieldCheck className="mr-1.5 size-3.5" />Setujui</Button> : null}{item.status === "PREPARED" && user?.role === "SHAREHOLDER" && item.preparedByUserId === user.id ? <Badge className="h-8 status-rejected">MAKER TIDAK DAPAT MENYETUJUI</Badge> : null}{item.status === "PREPARED" && user?.role !== "SHAREHOLDER" ? <Badge className="h-8 status-pending">MENUNGGU SHAREHOLDER</Badge> : null}{(item.status === "APPROVED" || item.status === "EXPORTED") ? <Button size="sm" variant="outline" onClick={() => { printIncident(item); exportIncident.mutate({ incidentId: item.id }); }}><FileDown className="mr-1.5 size-3.5" />Cetak / ekspor</Button> : null}</div></div>{canApprove ? <Textarea className="mt-4 min-h-20" placeholder="Catatan persetujuan Shareholder (opsional)" value={notes[item.id] ?? ""} onChange={(event) => setNotes((current) => ({ ...current, [item.id]: event.target.value }))} /> : null}{item.status === "APPROVED" || item.status === "EXPORTED" ? <div className="mt-4 flex gap-2 rounded-xl bg-indigo-50 px-3 py-2 text-xs leading-5 text-indigo-900"><LockKeyhole className="mt-0.5 size-4 shrink-0" />Cetak ini adalah bukti paket internal; tidak berarti regulator telah menerima laporan.</div> : null}</article>;})}</div></CardContent></Card>
  </section>;
}

function Field({ label, value, onChange, type = "text", placeholder }: { label: string; value: string; onChange: (value: string) => void; type?: string; placeholder?: string }) { return <div><Label>{label}</Label><Input type={type} placeholder={placeholder} className="mt-2 bg-white" value={value} onChange={(event) => onChange(event.target.value)} /></div>; }
function RowField({ label, value, onChange, placeholder }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string }) { return <div><Label>{label}</Label><Textarea placeholder={placeholder} className="mt-2 min-h-28 bg-white font-mono text-xs leading-5" value={value} onChange={(event) => onChange(event.target.value)} /></div>; }

function WorkbookBundleImport({ onImported }: { onImported: (value: { profitLossRows: FinancialRow[]; balanceSheetRows: FinancialRow[]; equityRows: FinancialRow[]; sourceStorageKey: string | null; sourceFileName: string; sourceMimeType: string }, sourceNames: string[]) => void }) {
  const [isImporting, setIsImporting] = useState(false);
  const uploadBundle = async (fileList: FileList | null) => { const files = Array.from(fileList ?? []); if (files.length !== 3) { toast.error("Pilih tepat tiga workbook: B0002, B0003, dan B0004."); return; } if (files.some((file) => file.size > 5 * 1024 * 1024)) { toast.error("Setiap workbook maksimal 5 MB."); return; } setIsImporting(true); try { const payloadFiles = await Promise.all(files.map(async (file) => { const dataBase64 = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = () => reject(new Error("Berkas tidak dapat dibaca.")); reader.readAsDataURL(file); }); const mimeType = file.type || (file.name.toLowerCase().endsWith(".xls") ? "application/vnd.ms-excel" : "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"); return { dataBase64, originalFileName: file.name, mimeType, byteSize: file.size }; })); const response = await fetch("/api/financial-snapshot-bundle-import", { method: "POST", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ files: payloadFiles }) }); const payload = await response.json() as { imported?: { profitLossRows: FinancialRow[]; balanceSheetRows: FinancialRow[]; equityRows: FinancialRow[]; sourceStorageKey: string; sourceFileName: string; sourceMimeType: string }; message?: string }; if (!response.ok || !payload.imported) throw new Error(payload.message || "Workbook tidak dapat digabungkan."); onImported(payload.imported, files.map((file) => file.name)); toast.success("Tiga workbook dipetakan dan digabungkan sebagai sumber audit. Periksa pos sebelum menyimpan snapshot."); } catch (error) { toast.error(error instanceof Error ? error.message : "Workbook tidak dapat digabungkan."); } finally { setIsImporting(false); } };
  return <Card className="border-[#cddcec] bg-[#f8fbff]"><CardContent className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold text-[#315879]">Punya tiga workbook terpisah?</p><p className="mt-1 text-xs leading-5 text-[#647a92]">Pilih B0002, B0003, dan B0004 sekaligus. Sistem hanya memetakan untuk pemeriksaan dan tidak menyimpan berkas atau snapshot sampai Anda menekan simpan.</p></div><label className="press-scale inline-flex h-9 cursor-pointer items-center justify-center rounded-md bg-[#e7f49a] px-3 text-sm font-semibold text-[#203a56] hover:bg-[#dff085]"><FileSpreadsheet className="mr-1.5 size-4" />{isImporting ? "Memetakan…" : "Pilih 3 workbook"}<input type="file" className="sr-only" multiple accept=".xlsx,.xls,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel" disabled={isImporting} onChange={(event) => { void uploadBundle(event.target.files); event.currentTarget.value = ""; }} /></label></CardContent></Card>;
}

function RoleWalkthroughGuide() {
  return <Card className="border-[#dce6f0]"><CardHeader><CardTitle className="font-display text-xl text-[#18395f]">Urutan kerja Controller dan Shareholder</CardTitle><CardDescription>Ikuti urutan ini untuk memisahkan pembuat, pemeriksa, dan tindakan ekspor manual.</CardDescription></CardHeader><CardContent><div className="grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-[#d9e7f4] bg-[#f8fbff] p-4"><p className="text-sm font-bold text-[#315879]">Controller</p><ol className="mt-3 list-decimal space-y-2 pl-4 text-xs leading-5 text-[#60758d]"><li>Pilih satu workbook gabungan atau tiga workbook B0002, B0003, dan B0004.</li><li>Periksa periode, nama sumber, setiap pos, dan angka yang dipetakan.</li><li>Simpan snapshot, buat draf paket, lalu pilih <strong>Siapkan</strong>.</li><li>Jangan menyetujui paket yang Anda siapkan sendiri.</li></ol></div><div className="rounded-xl border border-emerald-100 bg-emerald-50/40 p-4"><p className="text-sm font-bold text-emerald-800">Shareholder</p><ol className="mt-3 list-decimal space-y-2 pl-4 text-xs leading-5 text-[#527263]"><li>Periksa periode, sumber audit, ringkasan pos, dan catatan Controller.</li><li>Gunakan akun yang berbeda dari pembuat paket.</li><li>Setujui hanya jika isi sudah direkonsiliasi dan sesuai prosedur perusahaan.</li><li>Ekspor/cetak hanya sebagai paket manual; aplikasi tidak mengirim data ke regulator.</li></ol></div></div></CardContent></Card>;
}
