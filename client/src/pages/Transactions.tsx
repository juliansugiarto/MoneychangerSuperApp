import { formatIdrDecimal } from "@/lib/money";
import { toast } from "sonner";

/** Shared bon/kwitansi helpers used by both TransactionCreate.tsx (the form) and TransactionList.tsx (the archive). Kept in this file — not renamed — so existing tests that read Transactions.tsx source text for "printBon"/"window.print()" keep working. */

export type Customer = { id: number; fullName: string; cifNumber: string; phoneNumber?: string | null; identityType: string; identityNumber: string; address?: string | null; occupation?: string | null; sourceOfFunds?: string | null; transactionPurpose: string | null; hasBeneficialOwner?: boolean; beneficialOwnerCustomerId?: number | null };
export type DenominationRow = { value: string; quantity: string };
/** One row of the printed kwitansi's table (NO. / MATA UANG / JUMLAH / KURS / TOTAL). */
export type PrintableLine = { currencyCode: string; foreignAmount: string; agreedRate: string; rupiahAmount: string };

export const transactionStatusClass: Record<string, string> = { DRAFT: "status-pending", PENDING_REVIEW: "status-pending", APPROVED: "status-approved", COMPLETED: "status-approved", RETURNED: "status-rejected", CANCELLED: "status-rejected" };
export const transactionStatusLabel: Record<string, string> = { DRAFT: "Draft", PENDING_REVIEW: "Perlu review", APPROVED: "Disetujui", COMPLETED: "Selesai", RETURNED: "Dikembalikan", CANCELLED: "Dibatalkan" };
export const toBase64 = (file: File) => new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = reject; reader.readAsDataURL(file); });
export const escapeHtml = (value: unknown) => String(value ?? "-").replace(/[&<>\"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#039;" })[character] ?? character);
export const csvCell = (value: unknown) => `"${String(value ?? "").replaceAll('"', '""')}"`;

/** Renders a print window that matches the physical KWITANSI JUAL/BELI receipt book exactly, footer notice included verbatim. */
export function printBon(transaction: any, customer: Customer | null, lines: PrintableLine[]) {
  const win = window.open("", "_blank"); if (!win) return toast.error("Izinkan pop-up browser untuk menyimpan PDF atau mencetak kwitansi.");
  const isSell = transaction.operation === "SELL"; // SELL = kita jual valuta ke nasabah = KWITANSI JUAL
  const title = isSell ? "KWITANSI JUAL" : "KWITANSI BELI";
  const subtitle = isSell ? "SALES RECEIPT" : "PURCHASE RECEIPT";
  const accent = isSell ? "#1f7a44" : "#18395f";
  const name = transaction.customerFullNameSnapshot ?? customer?.fullName;
  const identity = `${transaction.customerIdentityTypeSnapshot ?? customer?.identityType ?? ""} ${transaction.customerIdentityNumberSnapshot ?? customer?.identityNumber ?? ""}`.trim();
  const phone = transaction.customerPhoneSnapshot ?? customer?.phoneNumber;
  const address = transaction.customerAddressSnapshot ?? customer?.address;
  const sourceOfFunds = transaction.sourceOfFundsSnapshot ?? customer?.sourceOfFunds;
  const representativeLine = transaction.customerActingAs === "REPRESENTATIVE" ? `<br><small>Diwakili oleh: ${escapeHtml(transaction.representativeName)} · ${escapeHtml(transaction.representativeIdentityNumber)}</small>` : "";
  const rows = lines.map((line, index) => `<tr><td>${index + 1}</td><td>${escapeHtml(line.currencyCode)}</td><td class=r>${escapeHtml(line.foreignAmount)}</td><td class=r>${escapeHtml(line.agreedRate)}</td><td class=r>${escapeHtml(formatIdrDecimal(line.rupiahAmount))}</td></tr>`).join("");
  win.document.write(`<!doctype html><title>${escapeHtml(transaction.receiptNumber ?? transaction.transactionNumber)}</title><style>body{font-family:Arial;color:#18395f;margin:28px;line-height:1.35}.head{display:flex;justify-content:space-between;align-items:flex-start;gap:16px}h2{margin:0;font-size:15px;letter-spacing:.04em}.title{color:${accent};font-weight:800;font-size:20px;margin:0}.subtitle{font-size:11px;color:#64748b;font-style:italic;margin:0}.no{font-size:20px;font-weight:800;text-align:right;white-space:nowrap}.no small{display:block;font-size:11px;font-weight:400;margin-top:4px}p{font-size:12px;margin:2px 0}table{border-collapse:collapse;width:100%;margin-top:14px}td{border:1px solid #94a7b8;padding:7px 9px;font-size:12px;vertical-align:top}.r{text-align:right}.grid{display:grid;grid-template-columns:1fr 1fr;gap:0 24px;margin-top:14px}.notice{margin-top:14px;font-size:10.5px}.rule{margin-top:8px;border:1px solid #94a7b8;padding:9px;font-size:10.5px}.sign{display:flex;justify-content:space-between;margin:110px 40px 48px;text-align:center;font-size:12px}.sign span{display:block;min-width:160px}.sign .space{display:block;height:70px}</style><div class=head><div><h2>IBUKOTA VALASINDO</h2><p>MONEY CHANGER</p></div><div style="flex:1"><p class=title>${title}</p><p class=subtitle>${subtitle}</p></div><div class=no>No: ${escapeHtml(transaction.receiptNumber ?? "-")}<small>Tanggal: ${escapeHtml(new Date(transaction.transactionAt).toLocaleDateString("id-ID"))}</small></div></div><p>Jl. Mangun Sarkoro No 35, Cianjur, Jawa Barat 43214 &middot; +62 263-265500 / +62 263-265600</p><div class=grid><div><p><b>Nama</b>: ${escapeHtml(name)}${representativeLine}</p><p><b>No. HP</b>: ${escapeHtml(phone)}</p><p><b>No. KTP/Paspor</b>: ${escapeHtml(identity)}</p><p><b>Alamat</b>: ${escapeHtml(address)}</p></div><div><p><b>Sumber Dana</b>: ${escapeHtml(sourceOfFunds)}</p><p><b>Tujuan Transaksi</b>: ${escapeHtml(transaction.transactionPurposeSnapshot)}</p><p><b>Cara Bayar</b>: ${escapeHtml(transaction.paymentMethod)}</p></div></div><table><tr><td><b>NO.</b></td><td><b>MATA UANG</b></td><td class=r><b>JUMLAH</b></td><td class=r><b>KURS</b></td><td class=r><b>TOTAL</b></td></tr>${rows}<tr><td colspan=4><b>JUMLAH TOTAL</b></td><td class=r><b>${escapeHtml(formatIdrDecimal(String(transaction.rupiahAmount)))}</b></td></tr></table><p class=notice>* Harap hitung kembali uang anda sebelum meninggalkan loket.<br>Komplain setelah meninggalkan loket tidak akan dilayani.<br>* wajib melengkapi semua data</p><div class=rule>Sesuai Ketentuan Bank Indonesia PBI No. 18/20/PBI/2016, Customer wajib memberikan fotocopy kartu Identitas diri, dan setiap transaksi minimum 10.000 USD Customer wajib memberikan informasi tujuan transaksi (underlying). Dengan ini Saya Menyatakan Bahwa transaksi ini belum mencapai senilai 10.000 USD</div><div class=sign><span>Teller<span class=space></span>__________________</span><span>Nasabah<span class=space></span>__________________</span></div><script>window.print()</script>`);
  win.document.close();
}
